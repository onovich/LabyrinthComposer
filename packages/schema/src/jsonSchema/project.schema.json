{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://labyrinth-composer.local/schemas/project.schema.json",
  "title": "Labyrinth Composer Project",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "project",
    "startSpaceId",
    "targetSpaceIds",
    "spaces",
    "connections",
    "gates",
    "tokens",
    "puzzles",
    "beats"
  ],
  "properties": {
    "schemaVersion": {
      "const": "0.1.0"
    },
    "project": {
      "$ref": "#/$defs/projectMetadata"
    },
    "startSpaceId": {
      "$ref": "#/$defs/id"
    },
    "targetSpaceIds": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/id"
      },
      "uniqueItems": true
    },
    "spaces": {
      "$ref": "#/$defs/spaceMap"
    },
    "connections": {
      "$ref": "#/$defs/connectionMap"
    },
    "gates": {
      "$ref": "#/$defs/gateMap"
    },
    "tokens": {
      "$ref": "#/$defs/tokenMap"
    },
    "puzzles": {
      "$ref": "#/$defs/puzzleMap"
    },
    "beats": {
      "$ref": "#/$defs/beatMap"
    },
    "rulePresetId": {
      "$ref": "#/$defs/id"
    },
    "ruleOverrides": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/rulePresetOverride"
      },
      "uniqueItems": true
    },
    "diagnosticExceptions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/diagnosticException"
      },
      "uniqueItems": true
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9_.:-]*$"
    },
    "stringArray": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "diagnosticSeverity": {
      "enum": ["error", "warning", "info"]
    },
    "entityRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "id"],
      "properties": {
        "kind": {
          "enum": ["space", "connection", "gate", "token", "puzzle", "beat"]
        },
        "id": {
          "$ref": "#/$defs/id"
        }
      }
    },
    "thresholdMap": {
      "type": "object",
      "additionalProperties": {
        "type": "number"
      }
    },
    "rulePresetOverride": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ruleId"],
      "properties": {
        "ruleId": {
          "$ref": "#/$defs/id"
        },
        "thresholdOverrides": {
          "$ref": "#/$defs/thresholdMap"
        },
        "severity": {
          "$ref": "#/$defs/diagnosticSeverity"
        },
        "disabled": {
          "type": "boolean"
        }
      }
    },
    "diagnosticException": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "ruleId", "entityRefs"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "ruleId": {
          "$ref": "#/$defs/id"
        },
        "entityRefs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/entityRef"
          },
          "minItems": 1
        },
        "reason": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        }
      }
    },
    "projectMetadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string"
        },
        "author": {
          "type": "string"
        }
      }
    },
    "space": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string"
        },
        "tags": {
          "$ref": "#/$defs/stringArray"
        }
      }
    },
    "connection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "fromSpaceId", "toSpaceId"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "fromSpaceId": {
          "$ref": "#/$defs/id"
        },
        "toSpaceId": {
          "$ref": "#/$defs/id"
        },
        "directed": {
          "type": "boolean"
        },
        "gateId": {
          "$ref": "#/$defs/id"
        },
        "description": {
          "type": "string"
        }
      }
    },
    "gate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "kind", "requiredTokenIds"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "enum": ["lock", "ability", "knowledge", "state", "resource"]
        },
        "requiredTokenIds": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true
        },
        "description": {
          "type": "string"
        }
      }
    },
    "token": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "kind"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "enum": ["item", "ability", "knowledge", "state", "relationship", "resource"]
        },
        "locationSpaceId": {
          "$ref": "#/$defs/id"
        },
        "description": {
          "type": "string"
        }
      }
    },
    "puzzle": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "locationSpaceId", "requiredTokenIds", "outputTokenIds"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "locationSpaceId": {
          "$ref": "#/$defs/id"
        },
        "requiredTokenIds": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true
        },
        "outputTokenIds": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/id"
          },
          "uniqueItems": true
        },
        "description": {
          "type": "string"
        }
      }
    },
    "beat": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name"],
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "spaceId": {
          "$ref": "#/$defs/id"
        },
        "kind": {
          "enum": ["discovery", "threat", "relief", "puzzle", "reward"]
        },
        "intensity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "order": {
          "type": "number"
        },
        "description": {
          "type": "string"
        }
      }
    },
    "spaceMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/space"
      }
    },
    "connectionMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/connection"
      }
    },
    "gateMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/gate"
      }
    },
    "tokenMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/token"
      }
    },
    "puzzleMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/puzzle"
      }
    },
    "beatMap": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/beat"
      }
    }
  }
}
